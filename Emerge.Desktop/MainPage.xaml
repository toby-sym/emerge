<Page
    x:Class="Emerge_Desktop.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Emerge_Desktop"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Canvas x:Name="SimulationCanvas"
                Grid.Row="0"
                Background="#FF101018" />

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Padding="12"
                    Spacing="12">
            <Button x:Name="StartButton" Content="▶ Start" Click="StartButton_Click" />
            <Button x:Name="PauseButton" Content="⏸ Pause" Click="PauseButton_Click" />
            <TextBlock x:Name="TickLabel" Text="Tick: 0" VerticalAlignment="Center" Margin="20,0,0,0" />
            <TextBlock x:Name="PopulationLabel" Text="Population: 0" VerticalAlignment="Center" Margin="20,0,0,0" />
            <TextBlock x:Name="AvgSpeedLabel" Text="Avg Speed: 0.00" VerticalAlignment="Center" Margin="20,0,0,0" />
        </StackPanel>
    </Grid>
</Page>